services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: f1-api
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app:/app/app
    environment:
      - DEBUG=True
      - APP_NAME=F1 Live AI Commentator
    restart: unless-stopped
